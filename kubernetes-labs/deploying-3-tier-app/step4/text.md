**Pod Logs**  
Check the logs of the 3 applications, see if they're operational.

**Hint:**  
Use the following command to create a yaml file  
`kubectl create <deployment_name> --image=<image> --dry-run=client -o yaml > file.yaml`   
