# Utility Scripts

This directory was made in order to have a single file containing all utility functions in one place  
basically any utility function here is hard linked to the asset directories in each question.