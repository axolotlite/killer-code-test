apiVersion: v1
kind: Pod
metadata:
  name: silent.tower.7e85.echo
  labels:
    app: broken-crown-a2f
    instance: 050382
    role: sentinel
spec:
  containers:
    - name: idle
      image: busybox:1.36
      command:
        - sh
        - -c
        - while true; do echo "silent.tower.7e85.echo drifts through the realm"; sleep 3600; done
  restartPolicy: Always