## Tasks  
From the Main Controlplane node do:  
* Get all the nodes in the cluster 
  * write them to `~/report/nodes.txt`  
* Write the worker node ip address into `~/reports/node01.txt`
* SSH into the worker node 
  
Once you open the ssh session, move onto the next stage.